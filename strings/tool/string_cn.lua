require("fns")

local strings = {
    -- CHARACTERS = {
    --     MANUTSAWEE = require("manutsawee"),
    --     GENERIC = require("generic"),
    -- },
}

merge_table(strings, require("common"))

return strings
